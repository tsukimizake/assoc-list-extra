{
  "scripts": {
    "test-format": "elm-format --validate src",
    "test": "elm-verify-examples && npm run test-format"
  },
  "devDependencies": {
    "elm-test": "^0.19.1",
    "elm-verify-examples": "^3.1.0"
  },
  "dependencies": {}
}
